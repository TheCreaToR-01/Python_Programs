import shutil, os

#shutil.copy(source, destination) copying one file from one point to another

#shutil.copytree(source, destination) copies whole folder

#shutil.move(source, destination) moving a file from one point to another

# Note: Destination should already exist

# os.unlink(path) delete the file at the path

#os.rmdir(path) delete the folder at the path(folder must be empty)

# shutil.rmtree(path) delete whole folder at path including the whole files in it
